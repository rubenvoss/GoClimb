<div data-edit-trip-target="infos">
  <div class="crag-photo"
      style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)),
      url('<%= cl_image_path trip.crag.photo.key, height: 300, crop: :fill %>')">
  </div>

  <div class = "trip-infos p-1 pt-3">
    <p><strong><%= trip.name %></strong></p>
    <p><em><%= trip.crag.name %></em></p>
    <p><%= trip.start_date %> ⇨ <%= trip.end_date %></p>
    <% if profile.user == current_user %>
      <button aria-label="Edit trip">
        <i class="fa-solid fa-pen fa-xs ml-2" data-action="click->edit-trip#displayForm"></i>
      </button>
    <% end %>
  </div>
</div>
